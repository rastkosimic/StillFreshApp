// Firebase configuration
// The actual runtime config is loaded from google-services.json (Android) and
// GoogleService-Info.plist (iOS) — these files are NOT committed to the repo.
// This file holds any additional Firebase client config needed by the JS layer.

export const FIREBASE_CONFIG = {
  // Populated at build time from environment / EAS secrets.
  // See: https://docs.expo.dev/push-notifications/push-notifications-setup/
};
